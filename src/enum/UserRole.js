/**
 * @type {{USER: string, ADMIN: string, PRO: string}}
 * @deprecated
 * Todo: use new system user roles.
 */

const UserRole = {
    USER: 'user',
    ADMIN: 'admin',
    PRO: 'pro',
};

module.exports = UserRole;